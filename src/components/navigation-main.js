import React from "react"

const NavigationMain = () => {
  const theMenu = "Menu"
  return (
    <div
      style={{
        fontSize: "14px",
        color: "#FFFFFF",
        textTransform: "uppercase",
        fontFamily: "inherit",
      }}
    >
      {theMenu}
    </div>
  )
}

export default NavigationMain
